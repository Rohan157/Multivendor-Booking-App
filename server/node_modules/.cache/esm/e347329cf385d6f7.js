let express,readdirSync,cors,mongoose;_f38‍.w("express",[["default",["express"],function(v){express=v}]]);_f38‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_f38‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_f38‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



require("dotenv").config();
const morgan = require("morgan");

const app = express();

//db connection
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
  })
  .then(() => console.log("DataBase Connected"))
  .catch((err) => _f38‍.g.console.log("DB Connection Error:", err));
//morgan is use here to show routes in console. it should only be used in development
//Remeber to remove it from here once development is done.
//Regards Rohan
//middleware
//cors is used to run frontend and backend simultaneously otherwise their linking can cause error
app.use(cors());
app.use(morgan("dev"));
//to see console logs having json data such as res.body in registeration
app.use(express.json());
//route middleware
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`Server is running on port ${port}`));
